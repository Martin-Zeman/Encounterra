#TODO Let's add a function to return all reachable map coordinates by a combatant regardless of distancei
# TODO In case of dodge it always makes sense to use it before movement. So connect it that way
# TODO In case of disengage it always makes sense to use it before movement. So connect it that way and then when adding up the threat don't add any from AoO

If I don't consider positioning for the depth==2 actions then it doesn't really make sense having a coord for each threat
level since the higher threat would always be picked. Or maybe not, since e.g. in case of ranged attacks one may be at a
disadvantage due to an adjacent enemy.
But considering them is hard to do since I'm not able to plan the path
from the previous coord.

Idea:
Positioning:
- For each action factory:
    - I calculate threat for each possible target
    - I find all admissible positionings of the actor (range of ability, impassable terrain etc.)
    - Compute dijkstra with the current combatant mask
    - Formulate it as a maximum flow problem where I connect all the positionings to a fake terminal node
    #- Each positioning is marked as a sink with the computed threat and solve Minimum-cost flow problem where the costs are represented by AoOs and AoE effects
-

Examples of AoE triggers:
- When a creature enters the spell’s area for the first time on a turn or starts its turn there, that creature must make a Constitution saving throw
- Each creature that is completely within the cloud at the start of its turn must make a Constitution saving throw against poison.
- Any creature that starts its turn in the area takes 2d6 cold damage. Any creature that ends its turn in the area must succeed on a Dexterity saving throw or take 2d6 acid damage as milky, otherwordly tentacles rub against it.
- One side of the wall, selected by you when you cast this spell, deals 5d8 fire damage to each creature that ends its turn within 10 feet of that side or inside the wall. A creature takes the same damage when it enters the wall for the first time on a turn or ends its turn there.
- Any creature that ends its turn within 5 feet of the sphere must make a Dexterity saving throw. The creature takes 2d6 fire damage on a failed save, or half as much damage on a successful one.


The action selection could be formulated as an integer linear programming problem. The optimization criteria could differ based on the type of a
character but in general it would maximize (threat_out - threat_in). I'd need to add a threat to a given specific combatant by a factory (already WIP).
This could help me determine the incoming threat. It could even handle threat from potential AoO.
I think I would need to include all combinations of action + targeting. For each such pair a all feasible required positioning would be calculated.
There would be Threat_in as a function of the positioning would be selected along with the positioning. I'd calculate both action and bonus
action and their positionings separately at first. Then pick one which I want to do first and then potentially recalculate the other.
(There would be a hard constraint that the threat_in cannot be higher or equal current hit points -> may not work)
So I'd calculate threat for all possible action and target combination (including dash)
    - I have a variable D_a,e how much dmg an attack "a" does to an enemy "n"
    - Variable which selects an action A
    - Variable which selects the i-th target T_i,a for action "a". Most actions have only one target.
    - each action would define its own constraints, e.g. for attacks you have to be adjacent
    - the constraints would be activated based on the selected action
    - There would be areas of threat projected by enemies determined by their range and calculated directly against the combatant
    - max (threat_out - threat_in)
    - stuff like reckless attack could be activated by a variable and add to both threat_out and threat_in...here the optimization becomes interesting
    - For each target the best route to the adjacent square would be calculated and AoO dmg added as an extra threat for that option
    Potential issues constraints:
        - I need to take conditions into account, both existing ones and ones potentially inflicted by one action
        - Need to deal with the order of action and bonus action. Some could have the constraints expressed as equations (like PaM bonus attack)
        - Would have to recalculate in case the
        - in case of hex the threat_out would only be activated if the other selected action was an attack-like action

I could improve pathfinding to give the path with the least AoO.


Longer lasting AoEs and multi-target effects are to be tracked by the EffectTracker.
It tracks the caster, location, who's currently affected as well as duration. It has a removal by broken concentration.
It can return any of the above. It can tell if a character is entering the area with location + increment.
it will be able to encode its state into a fixed size vector (maximum num combatants, each square binary vector for all aoes etc.)

buffs should not track themselves

(Before this would be decoding) Action is first run through the FeasibilityChecker and if feasible it's passed to the ResourceManager which
takes the resources away from the combatant. Then it's passed to the ActionFactory which creates the action itself and passes it to the
round_manager. The round manager resolves it either directly or passing it to the combat manager or effect_tracker.


FaurungDt vs 4 Cultists on Ubuntu:
 --------------STATISTICS--------------
 Team BLUE won total of 431 times
 Team RED won total of 569 times
---Simulation took 218.1 seconds ---


Trainign Faurung:
 Feasibility rate: 0.046; Unfeasibility rate 0.078
 Feasibility rate: 0.028; Unfeasibility rate 0.098
 Feasibility rate: 0.02; Unfeasibility rate 0.112
 Feasibility rate: 0.022; Unfeasibility rate 0.12
 Feasibility rate: 0.028; Unfeasibility rate 0.092
 Feasibility rate: 0.02; Unfeasibility rate 0.1
 Feasibility rate: 0.038; Unfeasibility rate 0.102

 Kinds of multiattacks:
 Horned Devil: The devil makes three melee attacks: two with its fork and one with its tails. It can use Hurl Flame in place of any melee
 attack
 Bone Devil: The devil makes three attacks: two with its claws and one with its sting
 Bearded Devil: the devil makes three melee attacks: one with its tail and two with its claws. Alternatively, it can use Hurl Flame twice
 Nalfeshnee: The nalfeshnee uses Horror Nimbus if it can. It then makes three attacks: one with its bite and two with its claws.
 Glabrezu: the glabrezu makes fours attacks: two with its pincers and two with its fists. Alternatively, it makes two attacks with its pincers
 and casts one spell.  -> this can be done by having two lists which we'd both check. When an action is selected, remove it from both lists. Remaining are still feasible.
 Chuul: the chuul makes two pincer attacks. If the chuul is grappling a creature, the chuul can also use its tentacles once
 Spined devil: The debil makes two attacks: one with its bite and one with its fork or two with its tail spines
 T-rex: the t-rex makes two attacks: one with its bite and one with its tail. It can't make both attacks against the same target ( restriction implemented in the t-rex itself)
 Adult Black Dragon: The dragon can use its Frightful presence. It then makes three attacks. One with its bite and two with its claws.
 Dragon turtle: The dragon turtle makes three attacks. One with its bite and two with its claws. It can make one tail attack in place of its
 two claw attacks.
 Drider: The drider makes three attacks, either with its longsword or its longbow. It can replace one of those attacks with a bite attack.
 Fomorian: the fomorian attacks twice with its greatclub or make one greatclub attack and uses Evil Eye once.
 Violet fungus: the fungus makes 1d4 rotting touch attacks
 Dao: the dao makes two fists attacks or two maul attacks.
 Gibbering Mouther: The gibbering mouther makes one bite attack and if it can, uses its blinding spittle
 Grick alpha: The grick makes two attacks, one with its tail and one with its tentacles. If it hits with its tentacles, the grick can
 make one beak attack against the same target
 Manticore: The manticore makes three attacks: one with its bite and two with its claws or three with its tail spikes.

 -> make a special category for these conditional attacks (like the gricks). They may have a target restriction.